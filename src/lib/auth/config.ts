/**
 * Authentication Configuration Constants
 */

/**
 * The cookie name used to store the user's session token.
 */
export const AUTH_COOKIE_NAME = 'diwrapp_session';

/**
 * Default session expiration duration in seconds (7 days).
 */
export const DEFAULT_SESSION_EXPIRY_SECONDS = 7 * 24 * 60 * 60; // 604,800 seconds

/**
 * Default session expiration duration in days.
 */
export const DEFAULT_SESSION_EXPIRY_DAYS = 7;
